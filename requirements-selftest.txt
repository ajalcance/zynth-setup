# The programs that judge a change to THIS repository — the self-test and the repo gate.
#
# The template refuses an unpinned scanner in a generated project (template/requirements-ci.txt,
# enforced by tests/guards/test_workflow_invariants.py). Until this file existed the template's
# own CI installed copier, jinja2, pyyaml and pre-commit unpinned, straight from the index, so a
# new copier release changed what the self-test proved with no diff anywhere. The rule a
# template ships is a rule its maintainers follow first.
#
# Used as a CONSTRAINTS file (`pip install -c requirements-selftest.txt <tool>`), not installed
# wholesale: each step installs only what it runs. Versions match the template's own pins where
# the tool is shared (pre-commit, pytest, pyyaml, zizmor, ruff, black), so the repository runs
# one version of each. Checked by `make policy` (template/scripts/check_pins.py); bumps arrive
# as Dependabot PRs.
copier==9.18.2
jinja2==3.1.6
pyyaml==6.0.3
pre-commit==4.6.2
pytest==9.1.1
ruff==0.16.8
black==26.5.1
zizmor==1.30.1
