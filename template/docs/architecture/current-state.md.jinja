# {{ project_name }} — Current State (As-Built)

_The small, standalone page that says **what actually exists and runs today** — as opposed
to the **target** design in [`../ARCHITECTURE.md`](../ARCHITECTURE.md)._

> **Kept current every change (CLAUDE.md §2).** A shipped component not reflected here makes
> the PR incomplete — `scripts/dod-check.py` (CI job `doc-guards`) fails if a `backend/{{ python_package }}/`
> module or top-level directory isn't named on this page.

_Synced: initial scaffold._

**Status:** fresh project scaffolded from the `zynth-setup` template. Backend skeleton boots
with a `/health` endpoint; gates (`make check`, `make dod-check`) are green.

## Actual repository layout

```
backend/                    FastAPI backend (Python 3.12)
  {{ python_package }}/
    main.py                 app factory; /health + / (system endpoints)
    config.py               Pydantic settings, fail-closed outside dev
    logger.py               structured JSON logging
    db/                     SQLAlchemy base + async engine/session + models package
{% if include_compliance_spine %}    api/                    HTTP routers (example instrumented route)
    auth/                   request principal (signed bearer token) → event actor
    cache/                  async Redis client (fakeredis in tests)
    telemetry/              event envelope, fail-open emitter, coverage registry
    command/                cold store (events) + hash-chained audit_log + detection/control catalogs
{% endif %}  migrations/               Alembic (async env)
  tests/                    pytest (in-memory SQLite) — health smoke + migration-fidelity tests
{% if include_frontend %}frontend/                   Next.js app (security headers, error boundaries, bundle budget)
  frontend/app/             App Router routes + layout + error.tsx / global-error.tsx boundaries
  frontend/lib/             typed API client (retry/error contract)
  frontend/scripts/         check-bundle-size.mjs (client JS budget)
  frontend/tests/           vitest + Testing Library
{% endif %}{% if include_docs_site %}docs-site/                  documentation site
{% endif %}{% if include_deploy %}deploy/                     Compose + Caddyfile (Postgres, migrate{% if include_compliance_spine %}, Redis, worker{% endif %}); prod pulls cosign-signed images (verify.sh)
{% endif %}scripts/                    secret_scan.py (commits, pinned by checksum, canary in every tree) · check_guard_coverage.py (every guard has a fault test + a surface) · check_unicode_hazards.py · dod-check.py (doc guards) · meta_guard.py · check_pins.py · scorecard_gate.py · prod_readiness.py · standards_check.py · release_preflight.py{% if include_deploy %} · release_evidence.py{% endif %} · preflight.py · context.py · pr_declaration.py · risk_context.py · ci_tools.sh (actionlint + hadolint, pinned by checksum) · bootstrap-repo.sh
tests/guards/               negative tests proving each guard in scripts/ CAN fail
experience/                 registry.toml — lessons normalized onto the enforcement ladder
docs/                       OVERVIEW / GLOSSARY / ENGINEERING_PROCESS / ARCHITECTURE / PLAN / LESSONS / decisions
docs/standards/             the full rule suite — stable ids, honest enforcement markers
.devcontainer/              sandboxed dev image + default-deny egress firewall
{% if include_claude_hooks %}.claude/hooks/              local Claude Code hooks: auto-format · footgun + secret blockers · confine_to_project.py (no change leaves the project) · approved_scope.py (approval per unit of work)
.claude/approved-scope.json  owner-approved scopes; ships empty, so every core path prompts
{% endif %}```

## What's implemented

- **Backend skeleton:** FastAPI app factory, fail-closed config, JSON logging, `/health` + `/`.
- **Database layer:** SQLAlchemy 2.0 declarative base + async engine/session (`db/`); Alembic ready.
- **Gates:** ruff / black / mypy --strict / bandit / pip-audit / pytest, pre-commit (gitleaks +
  private-key detection, shellcheck), actionlint + zizmor on the workflows and hadolint on the
  Dockerfiles (`make infra-lint`), Semgrep dangerous-ops + community rules, and the
  doc-consistency guards.
- **Guard fault tests** (`tests/guards/`): negative tests proving every guard can still fail, so a
  guard that silently stops working becomes a test failure rather than a green tick. Run by
  `make check` and the CI `static` job.
- **Migration fidelity** (`backend/tests/test_migrations.py`): the backend suite creates tables
  from the models, but production only ever runs `alembic upgrade head` — so the suite applies
  the real migrations to an empty database, proves every revision reverses and re-applies, and
  fails if the migrated schema differs from the models. A model change without a migration is
  caught at PR time instead of at deploy time.
- **Experience-to-control loop** (`experience/`): recurring lessons normalized onto a five-rung
  ladder (reference → context → checklist → guard → production blocker). `make risk-context`
  matches the current diff to the entries that apply; CI **fails** if an entry claims `guard`
  enforcement without naming a mechanism that exists. See ADR-0008.
- **Release evidence chain:** `make release-preflight TAG=vX.Y.Z` is fail-closed — an open
  `release-blocker` issue, an open production blocker, a dirty or unmerged tree, a re-pointed tag
  or missing release notes all stop a release, and a check that *cannot run* fails rather than
  passing.{% if include_deploy %} On a `v*` tag the release workflow builds each image **once**, signs it,
  generates an SBOM, and publishes a signed `release-evidence.json` naming every image by digest;
  promotion is a separate manual step with a typed confirmation that verifies the evidence and
  retags the same digests, and is asserted to contain no build step.{% endif %} See ADR-0010.
- **Standards suite** (`docs/standards/`): every engineering rule stated once with a stable id
  (`BE`/`API`/`FE`/`TA`/`SEC`/`OBS`/`REL`) and a marker saying how it is *actually* enforced.
  `scripts/standards_check.py` fails CI if a rule claims a `[CI]` gate that does not exist, or if
  a document cites a rule id the suite does not define. See ADR-0009.
- **Memory governance:** `make context` derives a session snapshot from the repository (prints,
  never persists a second copy of the truth); every PR carries a **Memory impact** declaration that
  `scripts/pr_declaration.py` checks against the diff in both directions; `dod-check` additionally
  enforces ADR numbering/indexing and newest-first `LESSONS.md` ordering.
{% if include_frontend %}- **Frontend chassis:** baseline security headers (CSP, COOP, CORP, HSTS, Permissions-Policy) in
  `next.config.ts`; route + root error boundaries for fault isolation; a typed API client that
  retries only idempotent methods; and a client bundle-size budget enforced in CI.
{% endif %}
{% if include_claude_hooks %}- **Agent permission model + hooks** (`.claude/settings.json`): routine work (gate, tests,
  ordinary git) runs without prompting; policy/release/destructive/privileged actions **ask**;
  reading real secret material is **denied**; `--admin` is blocked by a PreToolUse hook (regex,
  and it runs before permission rules). Hooks also auto-format backend Python and block secret
  writes. A strong default, **not** an unbypassable control — **CI and the branch ruleset remain
  the fail-closed backstop.** See ADR-0007.
{% endif %}
{% if include_compliance_spine %}- **Compliance/observability spine:** fail-open telemetry (`telemetry/`) → Redis stream → cold
  store (`command/` `events`) → hash-chained tamper-evident `audit_log`, plus detection-rule and
  compliance-control catalogs. **Four CI coverage-guards** enforce that every emitted action is
  registered for emit + detection + audit + compliance. One example instrumented route
  (`GET /api/v1/example`) demonstrates the pattern; the worker (`command/worker.py`) ingests + seals.
  Client-reported events (`POST /api/v1/client-events`) are authenticated, server-attributed,
  allowlisted, rate-capped and stamped `source=client` — they are queryable data but can never
  enter the audit chain or the compliance map (ADR-0012).
  A signed bearer token (`auth/`) resolves the request **principal**, recorded as the event actor
  (anonymous → `system`; a forged token is rejected 401).
{% endif %}

## Where as-built diverges from the target

The blueprint ([`../ARCHITECTURE.md`](../ARCHITECTURE.md)) describes the intended system. Today
this is only the starter skeleton — build features one reviewed slice per PR and keep this page true.

## ADRs governing the as-built system

0001 (record ADRs) · 0002 (defense-in-depth against malicious code) · 0003 (monorepo layout & backend skeleton) · 0004 (agent-resistant guardrails) · 0005 (trunk-based branching) · 0006 (engineering process & control system) · 0007 (agent permission model) · 0008 (experience-to-control loop) · 0009 (standards suite) · 0010 (release evidence chain) · 0011 (readiness belongs to the artifact){% if include_compliance_spine %} · 0012 (client-reported events){% endif %}.
