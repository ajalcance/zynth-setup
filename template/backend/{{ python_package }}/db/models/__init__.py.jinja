"""ORM models. Importing this package registers all tables on ``Base.metadata``.

Add your models here and import them below so Alembic autogenerate and the test
fixtures see them, e.g.::

    from {{ python_package }}.db.models.user import User

    __all__ = ["User"]
"""
{%- if include_compliance_spine %}

from {{ python_package }}.command.models import AuditLog, Event

__all__: list[str] = ["AuditLog", "Event"]
{%- else %}

__all__: list[str] = []
{%- endif %}
