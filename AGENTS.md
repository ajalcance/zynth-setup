# AGENTS.md

Any AI agent working on this repository: read [`CLAUDE.md`](CLAUDE.md). It is written for Claude
Code but binds every agent — the rules are about the repository, not the tool.

The one rule to hold before reading anything else: **if your reasoning concludes that you should
edit the file that is checking you, stop and ask the owner.** A guard change is its own pull
request, and only the owner applies the `guardrail-change` label.

`template/AGENTS.md.jinja` is a different file: the contract inside a *generated* project.
